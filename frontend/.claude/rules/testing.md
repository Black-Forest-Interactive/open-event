# Testing

## No UI tests

This project deliberately has no unit or E2E tests for the frontend. The UI is a thin client with minimal logic —
correctness is validated through backend tests and manual QA.

Do not suggest adding tests, test files, or test infrastructure to frontend code. Do not generate `*.spec.ts` files.
